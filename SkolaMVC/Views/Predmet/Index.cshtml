@model IEnumerable<SkolaMVC.Models.PredmetViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.PredmetId)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Naziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @* <td>
            @Html.DisplayFor(modelItem => item.PredmetId)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.Naziv)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new {  id=item.PredmetId  }) |
        @Html.ActionLink("Details", "Details", new {  id=item.PredmetId }) |*@
            @Html.ActionLink("Delete", "Delete", new { id = item.PredmetId })
        </td>
    </tr>
}

</table>
